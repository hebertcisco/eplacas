import { DefaultTheme } from "react-native-paper";

export const $THEME = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#007AFF",
    background: "#F2F2F7"
  }
};
export default $THEME;